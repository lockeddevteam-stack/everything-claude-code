13:  <link rel="manifest" href="/manifest.json"/>
70:    if("serviceWorker" in navigator){
72:        navigator.serviceWorker.register("/sw.js").catch(function(){});
1278: * LOCKED Push Notifications v1.0
1340:  /* Stable per-device id. Notifications are a property of this phone, not of
1413:    return "serviceWorker" in navigator &&
1415:      "Notification" in window;
1586:      var permission = Notification.permission;
1587:      if (permission === "default") permission = await Notification.requestPermission();
1590:          ? "Notifications are blocked for this site. Turn them back on in your browser or phone settings, then try again."
1591:          : "Notification permission was not granted.");
1594:      var reg = await navigator.serviceWorker.ready;
1599:      if (!publicKey) throw new Error("Notification service is not configured.");
1651:      var reg = await navigator.serviceWorker.ready;
1661:      "Notification" in window && Notification.permission === "granted";
1768:      permission: "Notification" in window ? Notification.permission : "unsupported",
1776:  if ("serviceWorker" in navigator) {
1777:    navigator.serviceWorker.addEventListener("message", function (e) {
1944:  if ("serviceWorker" in navigator) {
1945:    navigator.serviceWorker.ready.then(async function (reg) {
31756:function NotificationsCard() {
31776:      if (st.enabled) { await P.disable(); setNote("Notifications turned off."); }
31859:      }, "Notifications are blocked for this site in your browser or phone settings. Allow them there, then flip this on."));
32956:  })))), React.createElement(NotificationsCard, null), React.createElement("div", {
49945:    /* Notification deep-links land on a specific pane (?open=checkin) */
57482:  /* Notifications route into the app: a cold start carries ?open=…, and a tap
