# Coach — adversarial verification (08-build/coach.html)

Driven with Playwright (chromium at /opt/pw-browsers) against
`file:///…/08-build/coach.html`, all 17 dev states, TODAY = 2026-09-09,
figures recomputed from `08-build/fixtures.js` / `tests/fixtures/seed-data.json`.

## Fixed or sound

- Plan arithmetic is reproducible from the seed: "Week 6 of 12 / 50%" (start 2026-08-04 → wk 6), Build targets "Bench 5RM 72.5 of 75 = 97%" and "Deadlift 4RM 115 of 120 = 96%", Base "2 of 2 targets met" — all match the best sets in `lk_history` (bench 72.5×8, squat 87.5×8, deadlift 115×5 at reps ≥ the target floor).
- Peak's "Squat 1RM" correctly renders "Target 100 kg · Starts week 9 · Not started" rather than a percentage, so the 1RM target is never scored off an 8-rep set while the phase is future.
- Copy really copies: pressed `msg-copy-1`, toast "Reply copied.", `navigator.clipboard.readText()` returned "Your bench moved 60 to 72.5 kg for 5 in six weeks…".
- Sending persists: typed + sent, reply arrived, composer cleared to `""`; after reload the thread had 6 messages and `lk_coachLastMsgs` held them.
- Rename persists to both keys: saved "Gunnar" → `lk_coachName="Gunnar"` and `lk_profile.coachName="Gunnar"`; after reload the `<h1>` read Gunnar.
- Setup Save writes: instructions ("Only metric. No pep talk."), tone (`lk_coachStyle="technical"`), and a perm toggle (`lk_coachDataPrefs.nutrition=false`) all survived reload; the nutrition row came back `aria-checked="false"`.
- Memory add and clear-all write immediately (`lk_coachMemory` updated on press) and a `forget` + Save persists (`lk_coachMemory=[]` after Save).
- Chip dismissal persists: `lk_coachOpenersOff` gained the chip text and the chip stayed gone after reload; Undo emptied it again.
- Stop → "Stopped before I answered." + Continue, and Retry on the last reply, both re-fire and produce a reply matched to the question asked.
- Receipt expands in place ("✓ Training history … ✓ 1 saved memory ✓ Your instructions") and "Change what it sees" switches `data-view` to `setup`; the "9 of 9 sources" count tracks `lk_coachDataPrefs` (turning all off showed "0 of 9" in both places).
- Plan navigation leaves the screen for real: `plan-target-0` → progress.html, `plan-open-split` → split-builder.html, `plan-start` → train.html.

## Still open

1. **coach.html:1967 — "Save and edit them" saves nothing.** Ran the interview to preview, pressed `iv-edit-first`: toast "Saved. Edit them here.", actionbar "Saved just now", but `localStorage.lk_coachInstructions` was `null` and after reload the textarea reverted to the seed "Keep replies under 80 words…". The handler sets `S.setup.instructions` and `dirty=false` but never calls `persistSetup()` — the sibling `iv-use` does (verified: it wrote the full draft). The code comment above it claims "Saves first. The old build dropped the draft on this path."
2. **coach.html:1759 — binding a plan to a split is never written.** Saved the coach's plan from chat (`lk_coachPlan` written), then `plan-bind-open` → `bind-split-0`: screen changed to "Runs on PPL · 3 days · next is Push" and toasted "…runs on PPL. Push is next.", but stored `boundSplit` was still `null`; after reload the pane was back to "Before it can run". `bind-pick` mutates `S.plan.data` with no `persistPlan()`.
3. **coach.html:1771 — "Delete plan" does not delete.** With `lk_coachPlan` holding "8-week hypertrophy block", pressed Delete → "Delete plan": toast "Plan deleted.", pane emptied — stored `lk_coachPlan` still held the whole plan verbatim, and after reload the plan was back on the tab. `delete-confirm` never calls `persistPlan()`.
4. **coach.html:939, 963, 965, 985, 1985 — the interview says answers are kept; nothing is stored.** Answered 3 of 6, pressed Escape: toast "3 of 6 answers kept. Resume any time.", card read "3 of 6 answered. Nothing was lost." / "Resume at question 4". `Object.keys(localStorage)` was `[]`. After reload the card read "Start the interview" and all three answers were gone. Same false promise in the writing state ("Your six answers are already saved. Closing this does not lose them.") and the error state ("All six answers are kept on this phone").
5. **coach.html:306 — the check-in history is invented, not the seed's.** Screen shows 2 entries: "Yesterday — Energy 4 · Sleep 4 · Soreness 2 · Stress 2 · Motivation 4" and "Sun 6 Sep — Energy 3 · Sleep 3 · Soreness 4 · Stress 3 · Motivation 3". Seed `lk_feedback` has **6** entries (8/31–9/8); its 9/8 entry is energy 3, sleep 4, soreness 3, stress 2, mood 3 and its 9/6 entry is energy 3, sleep 3, soreness 2, stress 2, mood 3. No figure on the row matches, and `motivation` is not a field the key carries (the seed carries `mood`). The chat intro's "2 check-ins" is the same invented count.
6. **coach.html:464 — "over 7 weeks" is wrong.** Empty chat reads "I can see 18 sessions, over 7 weeks, 9 lifts with records and 2 check-ins." Lift history runs 2026-07-30 → 2026-09-07 = 39 days = 5.6 weeks; the code does `round(39/7)+1 = 7`. 18 sessions and 9 record lifts are correct (`totals.liftSessions`, `totals.recordLifts`); the span is not.
7. **coach.html:459 vs 684 — the intro claims data the same screen says is switched off.** Turned all nine sources off, Saved, then New chat: the receipt row read "Coach can see 0 of 9 sources" with, two inches above it, "I can see 18 sessions, over 7 weeks, 9 lifts with records and 2 check-ins." `introLine()` never consults `S.setup.perms`.
8. **coach.html:484 — "stalled" is asserted about a six-day-old record.** Empty chat offers "Why has my db shoulder press stalled at 25 kg?". The DB Shoulder Press PR in `lk_prs` is dated **2026-09-03**, six days before TODAY; every PR in the seed falls in 09-03…09-07, so nothing there is stale. The picker takes the oldest PR date unconditionally, and its comparator returns `1` for equal dates (Bench also sits on 09-03), so which lift is accused of stalling is decided by sort internals. The label is also raw-lowercased to "db shoulder press".
9. **coach.html:321/415/1311 — `lk_feedback` and `lk_coachPlan` are written in a shape the app's own key does not have, and the seed's value crashes the screen.** The check-in writes `[{"when":"Today","v":{energy,sleep,soreness,stress,motivation}}]` (verified in localStorage after a save); seed-data.json's `lk_feedback` is `[{id,date,dateStr,mood,energy,stress,sleep,soreness,hoursSlept,tags,note,anchored}]`. Injecting the seed's own row and reloading threw `Cannot read properties of undefined (reading 'energy')` (`e.v[f[0]]`, and `S.checkin.log[0].v.sleep` at :494) and the pane rendered nothing. Likewise injecting seed-data.json's `lk_coachPlan` (which uses `startDate`, no `weeks`/`boundSplit`, target values as strings "70 kg") threw `Cannot read properties of undefined (reading 'split')` at `parseDate(plan.start)` :415 and the Plan pane rendered nothing. The file's own header claims "Storage keys are the shipped app's own, so a screen reading lk_coachLastMsgs here is reading the key the real app reads."
10. **coach.html:2004 — typing instructions leaves the actionbar lying.** Typed " Extra." into the instructions textarea: the character counter updated to "64 / 2000" but the status still read "Saved just now" and the Save button still carried `disabled`. It only flipped to "Unsaved changes" 1.5 s later, when the unrelated once-a-second session-shelf interval (:2172) happened to repaint. The `input` handler updates state and the counter but never calls `render()`.
11. **coach.html:1832 — the memory switch silently commits other unsaved edits.** Picked tone "Brief" (unsaved — `lk_coachStyle` still `null`), then toggled "Use saved memories": `lk_coachStyle` became `"brief"` in storage while the actionbar still read "Unsaved changes". `memory-on` calls `persistSetup()`, which writes instructions, tone, memory and perms wholesale.
12. **coach.html:1708 — a saved plan card offers to save itself again after a reload.** From "Chat / plan offered", pressed "Save this plan": card became "Plan saved · … is on the Plan tab", `lk_coachPlan` written. After reload the same reply showed "Save this plan / Not now" again while the Plan tab correctly showed the 8-week block. `persistChat` stores the message's `offer` flag but not `offerSaved`, and `reset()` always sets `offerSaved:false`.
13. **coach.html:1784 — Undo takes effect in state but not on screen.** Pressed "New" (4 messages → empty), then the toast's Undo: 300 ms later the thread still showed 0 messages, the empty-state intro was still up and the toast was still on screen, while `lk_coachLastMsgs` already held the 4 restored messages; the thread reappeared only after the 1 s shelf tick. `'toast'` runs `fn()` and skips `render()`, and the `new-chat`, `edit-msg`, `dismiss-chip` and `clear-memory` undo callbacks do not render either.
14. **coach.html:1765 — "Open in Train" goes nowhere.** After binding, pressed the toast action: it replaced the toast with "Opening Upper / Lower in Train." and stayed on coach.html (url unchanged). The callback only toasts; the neighbouring `plan-start` does a real `location.href`.
15. **coach.html:1334, 1532 — check-in Save is offered on an incomplete form, and a saved day has no date.** With only Energy answered, `checkin-save` had no `disabled` attribute; pressing it toasted "4 still to answer: sleep, soreness, stress, motivation." and wrote nothing (`lk_feedback` null) — the handler early-returns, contradicting the comment at :1519 that claims the enabling was fixed. Separately the stored entry is `{"when":"Today","v":{…}}` with no date at all, and `checkin-save` replaces `log[0]` whenever its label is "Today" — so on any later day the pane reopens saying "Saved for today." with the old day's answers, and the next save overwrites that day instead of adding one.
