// Baseline page spec: split-builder. States, interactive crawl and error checks are defined in ../helpers.ts (PAGES['split-builder']).
import { pageSuite } from '../helpers';
pageSuite('split-builder');
