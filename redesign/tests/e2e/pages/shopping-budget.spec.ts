// Baseline page spec: shopping-budget. States, interactive crawl and error checks are defined in ../helpers.ts (PAGES['shopping-budget']).
import { pageSuite } from '../helpers';
pageSuite('shopping-budget');
