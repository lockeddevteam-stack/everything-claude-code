// Baseline page spec: exercise-library. States, interactive crawl and error checks are defined in ../helpers.ts (PAGES['exercise-library']).
import { pageSuite } from '../helpers';
pageSuite('exercise-library');
