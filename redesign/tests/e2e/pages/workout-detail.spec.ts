// Baseline page spec: workout-detail. States, interactive crawl and error checks are defined in ../helpers.ts (PAGES['workout-detail']).
import { pageSuite } from '../helpers';
pageSuite('workout-detail');
