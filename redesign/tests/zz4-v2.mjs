import {open,DEMO,T} from './zz4-lib.mjs';
const {b,p,errs}=await open(DEMO+'#/home');
await p.waitForTimeout(1400);
await p.click('[data-testid="open-account"]'); await p.waitForTimeout(1200);
await p.click('[data-testid="switch-cycle"]'); await p.waitForTimeout(600);
console.log('switch:',await p.getAttribute('[data-testid="switch-cycle"]','aria-checked'));
await p.locator('[data-testid="back"]').first().click(); await p.waitForTimeout(1200);
console.log('url',p.url());
await p.locator('[data-testid="row-cycle"]').first().click(); await p.waitForTimeout(1400);
console.log('D2 url',p.url());
console.log('D2 screen:',T(await p.locator('[data-testid="screen-cycle"]').first().innerText()).slice(0,160));
console.log('ERRS',errs);
await b.close();
